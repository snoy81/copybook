# Project Context

This directory contains a static English copybook generator.

- The original reference file is `house_words_red_trace_copybook.docx`.
- The reference DOCX embeds each A4 page as a full-page PNG image, not editable Word text.
- The first static implementation uses browser HTML/CSS preview and print-to-PDF export.
